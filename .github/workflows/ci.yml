name: ci

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  build-and-test:
    uses: DeanoC/ZiggySpiderweb/.github/workflows/ziggy-shared-ci.yml@main
    with:
      verify_script: |
        grep -q '\.name = \.ziggy_tool_runtime' build.zig.zon
        grep -q '\.version = ' build.zig.zon
