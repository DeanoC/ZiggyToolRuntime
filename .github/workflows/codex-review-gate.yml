name: codex-review-gate

on:
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
      - edited
      - ready_for_review
  pull_request_review:
    types:
      - submitted
      - edited
      - dismissed
  pull_request_review_comment:
    types:
      - created
      - edited
      - deleted
  pull_request_review_thread:
    types:
      - resolved
  issue_comment:
    types:
      - created
      - edited
      - deleted

permissions:
  contents: read
  pull-requests: read

jobs:
  codex-review-gate:
    if: ${{ github.event_name != 'issue_comment' || github.event.issue.pull_request != null }}
    runs-on: ubuntu-latest
    steps:
      - name: Validate codex review and comment resolution
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const prNumber = context.payload.pull_request?.number ?? context.payload.issue?.number;

            if (!prNumber) {
              core.setFailed('No pull request number found in event payload.');
              return;
            }

            const isCodexReviewer = (login) => {
              if (!login) return false;
              const normalized = login.toLowerCase();
              return normalized === 'chatgpt-codex-connector' ||
                normalized === 'chatgpt-codex-connector[bot]';
            };

            const reviews = await github.paginate(github.rest.pulls.listReviews, {
              owner,
              repo,
              pull_number: prNumber,
              per_page: 100,
            });

            const codexReviews = reviews.filter((review) => {
              if (!isCodexReviewer(review.user?.login)) return false;
              return review.state && review.state !== 'PENDING' && review.state !== 'DISMISSED';
            });

            if (codexReviews.length === 0) {
              core.setFailed('Missing required review from chatgpt-codex-connector.');
              return;
            }

            const latestCodexReview = [...codexReviews].sort((a, b) => {
              return new Date(a.submitted_at || 0) - new Date(b.submitted_at || 0);
            })[codexReviews.length - 1];

            if (latestCodexReview.state === 'CHANGES_REQUESTED') {
              core.setFailed('Latest chatgpt-codex-connector review is CHANGES_REQUESTED.');
              return;
            }

            const unresolvedThreads = [];
            let cursor = null;

            while (true) {
              const query = `
                query($owner: String!, $repo: String!, $number: Int!, $cursor: String) {
                  repository(owner: $owner, name: $repo) {
                    pullRequest(number: $number) {
                      reviewThreads(first: 100, after: $cursor) {
                        nodes {
                          id
                          isResolved
                          comments(first: 100) {
                            nodes {
                              author {
                                login
                              }
                              url
                            }
                          }
                        }
                        pageInfo {
                          hasNextPage
                          endCursor
                        }
                      }
                    }
                  }
                }
              `;

              const result = await github.graphql(query, {
                owner,
                repo,
                number: prNumber,
                cursor,
              });

              const reviewThreads = result.repository.pullRequest.reviewThreads;
              for (const thread of reviewThreads.nodes) {
                if (thread.isResolved) continue;
                const codexComment = thread.comments.nodes.find((comment) => isCodexReviewer(comment.author?.login));
                if (codexComment) {
                  unresolvedThreads.push(codexComment.url || thread.id);
                }
              }

              if (!reviewThreads.pageInfo.hasNextPage) break;
              cursor = reviewThreads.pageInfo.endCursor;
            }

            if (unresolvedThreads.length > 0) {
              core.setFailed(`Found ${unresolvedThreads.length} unresolved review thread(s) from chatgpt-codex-connector.`);
              return;
            }

            core.notice(`Codex review gate passed for PR #${prNumber}.`);
