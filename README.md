# ZiggyToolRuntime

Shared tool registry and tool execution runtime used by ZiggySpiderweb.

## Exports

- Tool schema/registry and provider export helpers
- Built-in world tool executor implementations

## Build

- `zig build`
- `zig build test`
